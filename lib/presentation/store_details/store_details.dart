import 'package:flutter/material.dart';

class StorDetailsView extends StatelessWidget {
  const StorDetailsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}